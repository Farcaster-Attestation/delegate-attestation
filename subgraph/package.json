{
  "name": "dynamic-attestation",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dynamic-attestation",
    "create-local": "graph create --node http://localhost:8020/ dynamic-attestation",
    "remove-local": "graph remove --node http://localhost:8020/ dynamic-attestation",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dynamic-attestation",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.1",
    "@graphprotocol/graph-ts": "0.38.1"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
